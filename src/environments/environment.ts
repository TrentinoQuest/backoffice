/**
 * Configurazione ambiente per lo sviluppo locale.
 * I valori per produzione/staging vanno in environment.prod.ts (TODO).
 */
export const environment = {
  production: false,
  apiBaseUrl: '/api/v1',
};
